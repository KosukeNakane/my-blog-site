###############################################
# .env.local.example (template)
#
# Copy to .env.local and adjust to your machine.
# This file is safe to commit publicly (no secrets).
###############################################

# Common defaults
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=change_me
DB_NAME=my-blog-site

# If you use MAMP, uncomment these instead:
# DB_PORT=8889
# DB_USER=root
# DB_PASSWORD=root

# Next.js will load .env.local automatically in dev
